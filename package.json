{
  "name": "themysticvision.com",
  "author": "José Roux",
  "scripts": {
    "start": "browser-sync start --server 'public/' --port 5000 --files 'public/**/*.html,public/**/*.js,public/**/*.css' --no-open --no-ghost-mode --inject-changes=false",
    "build": "rollup -c -w"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^11.1.1",
    "rollup": "^2.38.5",
    "rollup-plugin-terser": "^7.0.2"
  },
  "dependencies": {
    "lit-html": "^1.3.0"
  }
}
